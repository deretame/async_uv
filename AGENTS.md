常见坑（优先规避）：

1. 监听目录建议优先使用原生 Linux 文件系统路径（如 /tmp、/home），避免在虚拟挂载路径上运行 watcher 相关测试。
2. 含 watcher 的测试若出现卡住或超时，优先改到 /tmp 下启动测试二进制再复测。
3. Alpine 容器安装依赖时网络波动较大，apk update/add 需要重试机制，并优先替换到 cernet 镜像源。
4. 长时间容器流程（拉取/编译/测试）容易被中断，尽量复用已有容器，避免每次重新拉镜像。
